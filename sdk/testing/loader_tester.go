package testing

import "io"

func randStruct() io.Reader {
	// amount of fields
	// name of fields
	// datatype of fields
	// embedded fields?
	// random amount of markers on fields
	// random amount of markers on struct
	return nil
}
